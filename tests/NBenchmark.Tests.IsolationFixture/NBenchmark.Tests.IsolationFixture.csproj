<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>NBenchmark.Tests.IsolationFixture</RootNamespace>
    <TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <!-- For the scoped-DI fixtures. The worker resolves IServiceScopeFactory through *this*
         assembly's dependency graph, so the container's assemblies must be reachable from here. -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection"/>
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\NBenchmark\NBenchmark.csproj"/>
  </ItemGroup>
</Project>
