<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>NBenchmark.Tests</RootNamespace>
    <TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk"/>
    <PackageReference Include="xunit"/>
    <PackageReference Include="xunit.runner.visualstudio">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NBenchmark\NBenchmark.csproj"/>
    <ProjectReference Include="..\NBenchmark.Tests.ErrorFixtures\NBenchmark.Tests.ErrorFixtures.csproj"/>

    <!--
      The isolation fixture is a standalone executable that real-child-process tests launch via
      `dotnet exec`. ReferenceOutputAssembly=false because we never compile against it - we only
      need it built, and we need its own output directory (which has its runtimeconfig.json)
      rather than a copy of the dll next to the test assembly.
    -->
    <ProjectReference Include="..\NBenchmark.Tests.IsolationFixture\NBenchmark.Tests.IsolationFixture.csproj"
                     ReferenceOutputAssembly="false"/>

    <!--
      A target that needs the ASP.NET Core shared framework. Same rationale again, plus one of its
      own: referencing it would put this test assembly's own process under that framework, and the
      point of the fixture is to be a *different* process's problem.
    -->
    <ProjectReference Include="..\NBenchmark.Tests.WebFixture\NBenchmark.Tests.WebFixture.csproj"
                     ReferenceOutputAssembly="false"/>

    <!--
      The measurement worker, for the tests that spawn a real one. Same rationale as the fixture:
      we never compile against it, we need it built, and we need its own output directory because
      that is where its runtimeconfig.json and deps.json live.
    -->
    <ProjectReference Include="..\..\src\NBenchmark.Worker\NBenchmark.Worker.csproj"
                     ReferenceOutputAssembly="false"/>

    <!--
      The `dotnet benchmark` tool, for the tests that run it as a process. Not referenced for
      compilation: the tool is an entry point with top-level statements and nothing to call, and the
      only interesting question about it is what happens when it is started.
    -->
    <ProjectReference Include="..\..\src\NBenchmark.Tool\NBenchmark.Tool.csproj"
                     ReferenceOutputAssembly="false"/>
  </ItemGroup>

  <ItemGroup>
    <!--
      Baked in at build time so the tests never have to guess at relative bin paths. Both
      projects multi-target the same TFMs and build under the same configuration, so the test's
      own $(Configuration)/$(TargetFramework) always name the matching fixture output.
    -->
    <AssemblyMetadata Include="IsolationFixtureDirectory"
                      Value="$(MSBuildThisFileDirectory)..\NBenchmark.Tests.IsolationFixture\bin\$(Configuration)\$(TargetFramework)\"/>

    <AssemblyMetadata Include="WebFixtureDirectory"
                      Value="$(MSBuildThisFileDirectory)..\NBenchmark.Tests.WebFixture\bin\$(Configuration)\$(TargetFramework)\"/>

    <AssemblyMetadata Include="ToolDirectory"
                      Value="$(MSBuildThisFileDirectory)..\..\src\NBenchmark.Tool\bin\$(Configuration)\$(TargetFramework)\"/>

    <AssemblyMetadata Include="NBenchmarkWorkerDirectory"
                      Value="$(MSBuildThisFileDirectory)..\..\src\NBenchmark.Worker\bin\$(Configuration)\$(TargetFramework)\"/>
  </ItemGroup>

</Project>